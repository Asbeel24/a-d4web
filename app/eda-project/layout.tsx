import type { Metadata } from "next";
export const metadata: Metadata = { title: "EDA — 生成艺术实验", description: "选择一个实验，探索粒子、图形与交互系统。" };
export default function PageLayout({ children }: { children: React.ReactNode }) { return children; }
